#!/usr/bin/env bash
# bash -s stable < <(curl -s https://raw.github.com/binq/home-directory/master/bin/setup-unbuntu)

apt-get install git --quiet --assume-yes
apt-get install ruby-rvm --quiet --assume-yes
#rvm install 1.9.3
